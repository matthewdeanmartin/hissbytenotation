[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "hbn_rust"
version = "0.1.0"
description = "Rust-accelerated parser for Python literal expressions (hissbytenotation)"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
